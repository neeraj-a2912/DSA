package LinkedList;

// class GFG
// {
//     //This method returns the head of the LL after deleting node with value d.
//     public static Node deleteNode(Node head,int d)
//     {
//         //Add your code here.
//         Node currNode = head;
//         Node tobeDeleted = new Node(d);
//         while(currNode.next.data != tobeDeleted.data){
//             currNode = currNode.next;
//         }
//         currNode.next = currNode.next.next;
//         return head;
//     }
//     //This method returns the head node of the reversed Linked list.
//     public static Node reverse(Node head)
//     {
//         //Add your code here
//         Node currNode = head;
//         Node prev = null;
//         do{
//             Node next = currNode.next;
//             currNode.next = prev;
//             prev = currNode;
//             currNode = next;
//         }while (currNode != head);
        
//         currNode.next = prev;
//         head = prev;
//         return head;
//     }
// }

public class DeletionAndReverse {
    
}
